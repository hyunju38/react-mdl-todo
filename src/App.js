import React, { Component } from 'react';

class App extends Component {
    render () {
        let text = "Hello, world!";
        return (
            <h1>{text}</h1>
        );
    }
}

export default App;
